import React from "react";
// import "./new.scss";
import { withRouter } from "react-router-dom";

const Tap =  props => {
  return (
    <div
      className="Tap"
  >Tap</div>)}

  export default withRouter(Tap);