/**
 * Created by dev on 19.12.18.
 */

export default class TriangulationAlgorithm{

    /**
     * The method calculation and return triangle by vertices.
     * Returned data is Array of Arrays which contains three number, that numbers are index of vertices of polygon
     * @return {Array}
     */
    getTriangles(vertices){
        throw new Exception('The class doens\'t have implementation of the method');
    }
}
